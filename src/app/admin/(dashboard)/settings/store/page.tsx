import { AdminStoreSettingsClient } from "@/components/admin/admin-store-settings-client";

export const metadata = {
  title: "Store Settings | Admin",
};

export default function AdminStoreSettingsPage() {
  return <AdminStoreSettingsClient />;
}
